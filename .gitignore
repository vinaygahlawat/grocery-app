# Ignore node_modules directory, which contains installed dependencies
node_modules/

# Ignore the build output directory (typically "dist" for Vite)
dist/

# Local environment variable files
.env
.env.*.local
.env.local

# Log files
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Vite cache
.vite/

# Local IDE or editor configuration
.vscode/
.idea/

# macOS-specific files
.DS_Store

# Windows-specific files
Thumbs.db
ehthumbs.db

# Optional npm cache directory
.npm/

# Optional yarn cache directory
.pnp.*
.yarn/cache/
.yarn/unplugged/
.yarn/build-state.yml
.yarn/install-state.gz
.yarn/versions/

# Lint and formatting caches
.eslintcache
.stylelintcache

# TypeScript specific (if you are using TypeScript)
tsconfig.tsbuildinfo

# Ignore lock files (optional, depending on your preference)
# Uncomment if you want to ignore lock files like package-lock.json or yarn.lock
# package-lock.json
# yarn.lock

# Optional: Ignore testing coverage reports (if you're using a testing framework)
coverage/

# Optional: Ignore SASS cache files (if using SASS/SCSS)
.sass-cache/
